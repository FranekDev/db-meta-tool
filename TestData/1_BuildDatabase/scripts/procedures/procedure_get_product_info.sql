-- Osobny plik z pojedynczą procedurą
SET TERM ^ ;

CREATE PROCEDURE GET_PRODUCT_INFO (PROD_ID BIGINT)
RETURNS (
  PROD_NAME VARCHAR(100),
  PROD_PRICE DECIMAL(15,2)
)
AS
BEGIN
  SELECT NAME, PRICE
  FROM PRODUCTS
  WHERE ID = :PROD_ID
  INTO :PROD_NAME, :PROD_PRICE;
END^

SET TERM ; ^
